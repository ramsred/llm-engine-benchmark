# Benchmark Report

The executable report is generated by `./bench report` from accepted run artifacts under `results/`. The checked-in summary under `results/summaries/combined-summary.csv` is a compact preliminary portfolio snapshot.

Interpretation must keep cold and warm distributions separate, show units explicitly, and distinguish measured observations from causal hypotheses.
